[project]
name = "surfboard-exporter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["prometheus-client>=0.24.1", "beautifulsoup4>=4.12"]

[dependency-groups]
dev = ["pytest>=9.0.2", "pytest-watcher>=0.6.3", "watchdog>=6.0.0"]

[tool.pytest-watcher]
now = true
# clear = true
# notify_on_failure = false
# delay = 0.2
# runner = "pytest"
# runner_args = []
# patterns = ["*.py"]
# ignore_patterns = []
